services:
  db:
    image: postgres:16
    container_name: contratos_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: contratos
    ports:
      - "5433:5432"
    volumes:
      - contratos_pg:/var/lib/postgresql/data

volumes:
  contratos_pg:
